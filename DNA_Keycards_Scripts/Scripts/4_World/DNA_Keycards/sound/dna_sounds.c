class DNA_Sounds
{	
	static void SoundKCCrateSmolOpenPlay(vector pos)
	{
		EffectSound sound =	SEffectManager.PlaySound( "DNA_KeycardOpenSmol_SoundSet", pos );
		sound.SetSoundAutodestroy( true );
	}
	
	static void SoundKCCrateOpenPlay(vector pos)
	{
		EffectSound sound =	SEffectManager.PlaySound( "DNA_KeycardOpen_SoundSet", pos );
		sound.SetSoundAutodestroy( true );
	}
	
	static void SoundKCCrateOpenNoClickPlay(vector pos)
	{
		EffectSound sound =	SEffectManager.PlaySound( "DNA_KeycardOpenNoClick_SoundSet", pos );
		sound.SetSoundAutodestroy( true );
	}
	
	static void SoundKCCardScanPlay(vector pos)
	{
		EffectSound sound =	SEffectManager.PlaySound( "DNA_KeycardScan_SoundSet", pos );
		sound.SetSoundAutodestroy( true );
	}
	
	static void SoundKCCardRemovePlay(vector pos)
	{
		EffectSound sound =	SEffectManager.PlaySound( "DNA_KeycardRemove_SoundSet", pos );
		sound.SetSoundAutodestroy( true );
	}
	
	static void SoundKCAlarmBreachPlay(vector pos)
	{
		EffectSound sound =	SEffectManager.PlaySound( "DNA_BreachAlarm_SoundSet", pos );
		sound.SetSoundAutodestroy( true );
	}
	
	static void SoundKCCardInsertPlay(vector pos)
	{
		EffectSound sound =	SEffectManager.PlaySound( "DNA_KeycardInsert_SoundSet", pos );
		sound.SetSoundAutodestroy( true );
	}
	
	static void SoundKCCardProgramPlay(vector pos)
	{
		EffectSound sound =	SEffectManager.PlaySound( "DNA_KeycardProgram_SoundSet", pos );
		sound.SetSoundAutodestroy( true );
	}
	
	static void SoundKCCardKeyboardPlay(vector pos)
	{
		EffectSound sound =	SEffectManager.PlaySound( "DNA_KeycardKeyboard_SoundSet", pos );
		sound.SetSoundAutodestroy( true );
	}
	
	static void SoundKCCardSolderPlay(vector pos)
	{
		EffectSound sound =	SEffectManager.PlaySound( "DNA_KeycardSolder_SoundSet", pos );
		sound.SetSoundAutodestroy( true );
	}
	
	static void SoundKCOpenDrawerPlay(vector pos)
	{
		EffectSound sound =	SEffectManager.PlaySound( "DNA_KeycardOpenDrawer_SoundSet", pos );
		sound.SetSoundAutodestroy( true );
	}
	
	static void SoundKCCloseDrawerPlay(vector pos)
	{
		EffectSound sound =	SEffectManager.PlaySound( "DNA_KeycardCloseDrawer_SoundSet", pos );
		sound.SetSoundAutodestroy( true );
	}
	
	static void SoundKCAddBatteryPlay(vector pos)
	{
		EffectSound sound =	SEffectManager.PlaySound( "DNA_AddBattery_SoundSet", pos );
		sound.SetSoundAutodestroy( true );
	}
	
	static void SoundKCAddBatteryDeadPlay(vector pos)
	{
		EffectSound sound =	SEffectManager.PlaySound( "DNA_AddBatteryDead_SoundSet", pos );
		sound.SetSoundAutodestroy( true );
	}
	
	static void SoundKCRemoveBatteryPlay(vector pos)
	{
		EffectSound sound =	SEffectManager.PlaySound( "DNA_RemoveBattery_SoundSet", pos );
		sound.SetSoundAutodestroy( true );
	}
	
	static void SoundKCRemoveBatteryDeadPlay(vector pos)
	{
		EffectSound sound =	SEffectManager.PlaySound( "DNA_RemoveBatteryDead_SoundSet", pos );
		sound.SetSoundAutodestroy( true );
	}
	
	static void SoundKC40vBatteryDiedPlay(vector pos)
	{
		EffectSound sound =	SEffectManager.PlaySound( "DNA_40vBatteryDied_SoundSet", pos );
		sound.SetSoundAutodestroy( true );
	}
	
	static void SoundKCCardAttachPlay(vector pos)
	{
		EffectSound sound =	SEffectManager.PlaySound( "DNA_KeycardAttachHelpHand_SoundSet", pos );
		sound.SetSoundAutodestroy( true );
	}
	
	static void SoundKCCardDetachPlay(vector pos)
	{
		EffectSound sound =	SEffectManager.PlaySound( "DNA_KeycardDetachHelpHand_SoundSet", pos );
		sound.SetSoundAutodestroy( true );
	}
}